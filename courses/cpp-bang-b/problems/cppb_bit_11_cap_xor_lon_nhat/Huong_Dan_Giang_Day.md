# Hướng Dẫn Giảng Dạy: Maximum XOR Pair (Bit Greedy)
- Xét từng bit từ 30 về 0.
- Giả định bit hiện tại của đáp án có thể bật lên 1 hay không.
- Dùng sắp xếp mảng tiền tố bit và tìm kiếm nhị phân để kiểm tra trong $\mathcal{O}(N \log N)$.
- Độ phức tạp: $\mathcal{O}(30 \times N \log N)$.
