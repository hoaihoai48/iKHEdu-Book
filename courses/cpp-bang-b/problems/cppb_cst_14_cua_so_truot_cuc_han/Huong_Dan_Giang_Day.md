# Hướng Dẫn Giảng Dạy: Đếm Đoạn Con Tổng Trong [A, B]
- Số đoạn có tổng $\in [A, B] = (\text{Số đoạn tổng} \le B) - (\text{Số đoạn tổng} \le A - 1)$.
- Dùng hàm `count_at_most(limit)` chạy Sliding Window 2 lần trong $\mathcal{O}(N)$.
