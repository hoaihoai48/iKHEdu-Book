# Hướng Dẫn Giảng Dạy: Chênh Lệch Max-Min Trong Đoạn Con
- Với $N \le 5000$, ta có thể duyệt mở rộng $R$ từ $L$, duy trì `cur_min` và `cur_max` trong $\mathcal{O}(N^2)$ mà không cần cấu trúc dữ liệu nâng cao.
