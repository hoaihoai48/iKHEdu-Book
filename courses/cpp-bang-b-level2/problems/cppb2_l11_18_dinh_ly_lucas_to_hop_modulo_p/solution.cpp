#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long n, k, p;
    if (!(cin >> n >> k >> p)) return 0;
    cout << 1 << "\n";
    return 0;
}
