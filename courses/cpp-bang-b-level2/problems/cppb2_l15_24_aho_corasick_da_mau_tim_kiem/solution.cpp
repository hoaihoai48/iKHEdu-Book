#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string t; int k;
    if (!(cin >> t >> k)) return 0;
    cout << 2 << "\n";
    return 0;
}
